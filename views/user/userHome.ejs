<!DOCTYPE html>
<html>
  <%- include('../partials/head.ejs') %>
  <body>
    <!-- nav bar -->
    <%- include('../partials/userNavBar.ejs') %>

    <div class="container" id="mainDiv">
      <h5>Mobile</h5>
      <div class="mt-2 d-flex" style="overflow-x: auto; scrollbar-width: none">
        <%for(product of products){ %>
            <a href="/product/view/<%=product._id%>" class="text-dark">
        <div style="float: left; display: inline">
          <div class="card" style="width: 18rem">
            <img
              class="card-img-top mx-auto"
              src="./images/<%=product._id%>.jpg"
              alt="Card image cap"
              style="width: 8rem;"
            />
            <div class="card-body">
              <h5 class="card-title"><%=product.name%></h5>
              <p class="card-text">Price : <%= product.price%></p>
              <!-- <a href="/product/view/<%=product._id%>" class="btn btn-primary"></a> -->

            </div>
          </div>
        </div>
          </a>
        <%}%>
      </div>
    </div>

    <script defer>

    //  function viewProduct(id) {
    //   console.log(id);
    //     fetch(`http://127.0.0.1:3000/product/view/${id}`, {
    //       method: "GET",
    //     }).then(product=>{
    //       return product.json()
    //     }).then(product=>{
    //       console.log(product)
    //       document.getElementById("mainDiv").innerHTML = product._id
    //     })
    //  } 
    </script>
  </body>
</html>
